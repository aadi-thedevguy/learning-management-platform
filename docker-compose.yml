services:
  db:
    image: postgres:18
    hostname: localhost
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_USER=postgres
      - POSTGRES_DB=learning-management-platform
    volumes:
      - pgdata:/var/lib/postgresql
volumes:
  pgdata:
